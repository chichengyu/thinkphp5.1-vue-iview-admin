const types = {
	SET_TOKEN:'token',// 定义存时的 key
	SET_USER:'userInfo',// 定义存时的 key
	SET_NEWROUTES:'newRoutes',// 过滤后的路由新数组
	SET_SIDERLIST:'siderList',// 左侧菜单列表
	SET_RULES:'rules',// 存放用户组 如：['admin','editor']
}

export default types